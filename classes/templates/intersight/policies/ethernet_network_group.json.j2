{
  "Description": "{{ item.description | default('') }}",
  "Name": "{{ name_prefix ~ item.name ~ name_suffix }}",
  "ObjectType": "fabric.EthNetworkGroupPolicy",
  "Organization": {
    "Moid": "{{ org_moids[organization].moid }}",
    "ObjectType": "organization.Organization"
  },
{% if item.tags | length > 0 %}
  "Tags": [
{% for tag in item.tags %}
    {
{% if tag.tag_type | default('KeyValue') == 'PathTag' %}
      "Key": "{{ tag.key }}",
      "Type": "PathTag"
{% else %}
      "Key": "{{ tag.key }}",
      "Type": "KeyValue",
      "Value": "{{ tag.value }}"
{% endif %}
    }{% if not loop.last %},{% endif %}
{% endfor %}
  ],
{% else %}
  "Tags": [],
{% endif %}
  "VlanSettings": {
    "AllowedVlans": "{{ item.allowed_vlans | default('1001') }}",
    "NativeVlan": {{ item.native_vlan | default(1) }},
    "ObjectType": "fabric.VlanSettings",
    "QinqEnabled": {% if item.qinq_vlan is defined %}true{% else %}false{% endif %},
    "QinqVlan": {{ item.qinq_vlan | default(2) }}
  }
}
