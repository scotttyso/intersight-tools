{
  "BaudRate": {{ item.baud_rate | default(115200) }} ,
  "ComPort": "{{ item.com_port | default('com0') | lower }}",
  "Description": "{{ item.description | default('') }}",
  "Enabled": {{ item.enabled | default(true) | lower }},
  "Name": "{{ name_prefix ~ item.name ~ name_suffix }}",
  "ObjectType": "sol.Policy",
  "Organization": {
    "Moid": "{{ org_moids[organization].moid }}",
    "ObjectType": "organization.Organization"
  },
  "SshPort": {{ item.ssh_port | default(2400) }},
{% if item.tags | length > 0 %}
  "Tags": [
{% for tag in item.tags %}
    {
{% if tag.tag_type | default('KeyValue') == 'PathTag' %}
      "Key": "{{ tag.key }}",
      "Type": "PathTag"
{% else %}
      "Key": "{{ tag.key }}",
      "Type": "KeyValue",
      "Value": "{{ tag.value }}"
{% endif %}
    }{% if not loop.last %},{% endif %}
{% endfor %}
  ]
{% else %}
  "Tags": []
{% endif %}
}
