---
kind: Secret
apiVersion: v1
metadata:
  name: "{{ cluster.name }}-pull-secret"
  namespace: {{ cluster.name }}
data:
  '.dockerconfigjson': '{{ cluster.pull_secret }}'
type: 'kubernetes.io/dockerconfigjson'
