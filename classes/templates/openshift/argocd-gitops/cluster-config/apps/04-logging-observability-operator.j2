apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: logging-observability-operator
  namespace: openshift-gitops
spec:
  project: default
  source:
    repoURL: {{ argo_cd.repo_url }}
    targetRevision: HEAD
    path: cluster-config/openshift-logging/observability-operator/operator/overlays/stable
  destination:
    server: https://kubernetes.default.svc
    namespace: openshift-cluster-observability-operator
  syncPolicy:
    automated:
      prune: {{ argo_cd.sync_policy.prune | default(true) }}
      selfHeal: {{ argo_cd.sync_policy.self_heal | default(true) }}
    syncOptions:
      - CreateNamespace=true
